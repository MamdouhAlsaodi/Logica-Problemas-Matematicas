# Environment Variables Template
# Create a .env file in the backend directory and fill in the values below.

# Port the server will run on (default: 5000)
PORT=5000

# MongoDB connection URI
# Example for local: mongodb://localhost:27017/math_platform
MONGODB_URI=mongodb://localhost:27017/math_platform

# Node Environment (development or production)
NODE_ENV=development
